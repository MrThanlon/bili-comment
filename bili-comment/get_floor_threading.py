#-*- coding:utf-8 -*-
#软件多线程速度测试
#使用threading模块，在主线程中杀掉子线程

import threading

import get_floor
#4/2：只循环读取楼层，不做判断处理，测速
def floor_st() :
    pass
    get_floor.get_floor(av_number)
def floor_cycle(av_number , floor , thread_total) :
    
    threading.Thread(target = floor_st , name = )

if __name__ == '__main__' :
    pass
    